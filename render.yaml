services:
  - type: web
    name: gateway-monitor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    envVars:
      - key: CHIRPSTACK_API_KEY
        sync: false
